// 1. Основы JavaScript
// Создание переменной с именем и вывод в консоль
let myName = "Даня Пантин";
console.log(myName);

// Создание массива чисел и цикл для вывода каждого элемента
let numbers = [1, 2, 3, 4, 5];
for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
